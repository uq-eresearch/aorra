# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           @controllers.Application.index
POST    /login                      @controllers.Application.postLogin
GET     /user-exists                @controllers.Application.userExists
GET     /user-unverified            @controllers.Application.userUnverified
GET     /user/:email/verify/:token  @controllers.Application.verify(email: String, token: String)
POST     /user/:email/verify/:token @controllers.Application.postVerify(email: String, token: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET     /download/*file             @controllers.FileStoreController.download(file)
POST    /upload/                    @controllers.FileStoreController.postUpload(folder="")
POST    /upload/*folder             @controllers.FileStoreController.postUpload(folder)

GET     /filestore/notifications    @controllers.FileStoreAsync.notifications

# play-authenticate
GET     /logout                     com.feth.play.module.pa.controllers.Authenticate.logout
GET     /authenticate/:provider     com.feth.play.module.pa.controllers.Authenticate.authenticate(provider: String)
GET     /authenticate/:provider/denied     @controllers.Application.oAuthDenied(provider: String)

GET    /chart/*path                 @controllers.Chart.charts(path)
