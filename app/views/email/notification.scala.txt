@(
  events: List[service.filestore.EventManager.Event], 
  items: Map[String, service.filestore.FileStore.FileOrFolder]
)
@import java.net.URL
@import _root_.notification.NotificationManager.absUrl

The following changes were recently made in the AORRA system:
@for(event <- events) {
@(event.info.`type`) "@(items.get(event.info.id).getPath())" @(event.`type`)d
@absUrl(event)
}
